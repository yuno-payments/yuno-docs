---
title: "Yuno Documentation"
description: "Accept payments through one API. Connect to any provider, route intelligently, go live fast."
---

# Integrate Yuno

Connect to any payment provider through one API. Create a checkout session, collect payment details with our SDK, and process the payment — all in sandbox before you touch production.

<div className="flex flex-wrap gap-3 mt-6 mb-8">
  <a href="/docs/sdks/overview/quickstart" className="inline-flex items-center gap-2 px-5 py-2.5 rounded-lg text-sm font-semibold no-underline transition-colors" style={{ backgroundColor: 'var(--yuno-blue, #3E4FE0)', color: 'white' }}>
    Sandbox quickstart →
  </a>
  <a href="/reference/getting-started/api-reference-overview" className="inline-flex items-center gap-2 px-5 py-2.5 border border-gray-300 dark:border-gray-600 rounded-lg text-sm font-medium text-gray-700 dark:text-gray-200 no-underline hover:border-gray-400 dark:hover:border-gray-500 transition-colors">
    API reference
  </a>
  <span className="text-gray-400 dark:text-gray-500 self-center mx-1">·</span>
  <a href="/setup-mcp" className="text-sm text-gray-500 dark:text-gray-400 no-underline hover:text-gray-700 dark:hover:text-gray-200 self-center">Setup MCP</a>
  <a href="/skill.md" className="text-sm text-gray-500 dark:text-gray-400 no-underline hover:text-gray-700 dark:hover:text-gray-200 self-center">skill.md</a>
</div>

---

**Sandbox hello world** — amounts in major units, headers lowercase.

<Tabs>
  <Tab title="Node.js">
```javascript
// 1. Create checkout session (server)
const session = await fetch("https://api-sandbox.y.uno/v1/checkout/sessions", {
  method: "POST",
  headers: {
    "public-api-key": process.env.YUNO_PUBLIC_KEY,
    "private-secret-key": process.env.YUNO_SECRET_KEY,
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    account_id: process.env.YUNO_ACCOUNT_ID,
    merchant_order_id: "order-123",
    payment_description: "Test payment",
    country: "US",
    amount: { currency: "USD", value: 10 }  // $10.00
  })
}).then(r => r.json());

// 2. Create payment (in SDK callback, with one-time token)
const payment = await fetch("https://api-sandbox.y.uno/v1/payments", {
  method: "POST",
  headers: {
    "public-api-key": process.env.YUNO_PUBLIC_KEY,
    "private-secret-key": process.env.YUNO_SECRET_KEY,
    "X-Idempotency-Key": crypto.randomUUID(),
    "Content-Type": "application/json"
  },
  body: JSON.stringify({
    account_id: process.env.YUNO_ACCOUNT_ID,
    description: "Test payment",
    country: "US",
    merchant_order_id: "order-123",
    amount: { currency: "USD", value: 10 },
    checkout: { session: session.checkout_session },
    payment_method: { token: oneTimeToken }
  })
}).then(r => r.json());
// payment.status === "SUCCEEDED"
```
  </Tab>
  <Tab title="cURL">
```bash
# 1. Create checkout session
curl -X POST https://api-sandbox.y.uno/v1/checkout/sessions \
  -H "public-api-key: $YUNO_PUBLIC_KEY" \
  -H "private-secret-key: $YUNO_SECRET_KEY" \
  -H "Content-Type: application/json" \
  -d '{"account_id":"'"$YUNO_ACCOUNT_ID"'","merchant_order_id":"order-123",
       "payment_description":"Test payment","country":"US",
       "amount":{"currency":"USD","value":10}}'

# 2. Create payment
curl -X POST https://api-sandbox.y.uno/v1/payments \
  -H "public-api-key: $YUNO_PUBLIC_KEY" \
  -H "private-secret-key: $YUNO_SECRET_KEY" \
  -H "X-Idempotency-Key: $(uuidgen)" \
  -H "Content-Type: application/json" \
  -d '{"account_id":"'"$YUNO_ACCOUNT_ID"'","description":"Test payment",
       "country":"US","merchant_order_id":"order-123",
       "amount":{"currency":"USD","value":10},
       "checkout":{"session":"<checkout_session>"},
       "payment_method":{"token":"<one_time_token>"}}'
```
  </Tab>
</Tabs>

[Quickstart](/docs/sdks/overview/quickstart) · [Integration checklist](/docs/how-yuno-works/integration-checklist) · [Testing](/docs/how-yuno-works/testing)

---

## Choose your path

<CardGroup cols={2}>
  <Card title="SDK checkout" icon="code" href="/docs/sdks/overview/quickstart">
    Pre-built UI, no PCI scope. Web, iOS, Android.
  </Card>
  <Card title="Direct API" icon="server" href="/docs/direct-integration-use-cases/direct-flow">
    Full control over UI and flow. Requires PCI compliance.
  </Card>
  <Card title="Connect & route" icon="route" href="/docs/using-yuno/dashboard-overview/connections">
    Add providers, configure routing rules, enable methods.
  </Card>
  <Card title="Agents" icon="robot" href="/setup-mcp">
    MCP server, skill.md, llms.txt for AI coding assistants.
  </Card>
</CardGroup>

---

<Info>
**Building with an agent?** Start from [`/skill.md`](/skill.md) for workflow examples and [`/llms.txt`](https://docs.y.uno/llms.txt) for the full page index. Test cards are on [Testing Gateway](/docs/direct-integration-use-cases/yuno-testing-gateway) only.
</Info>

**Also:** [What is Yuno](/docs/how-yuno-works/what-is-yuno) · [Payment flow](/docs/how-yuno-works/how-yuno-payment-flow-works) · [Webhooks](/docs/webhooks/index) · [Changelog](/changelog) · [llms.txt](https://docs.y.uno/llms.txt)
