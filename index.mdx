---
title: "Yuno Documentation"
description: "Accept payments through one API. Connect to any provider, route intelligently, go live fast."
mode: "custom"
---

import { HeroSection, CodeCardHeader, CodeCardWrapper, CodeCaption } from '/snippets/homepage/HeroSection';
import { PathCards, AgentCallout, FooterLinks } from '/snippets/homepage/FeatureCards';

<HeroSection />

<CodeCardHeader title="sandbox · POST /v1/checkout/sessions" />

<CodeCardWrapper>

```bash
curl -X POST https://api-sandbox.y.uno/v1/checkout/sessions \
  -H "public-api-key: $YUNO_PUBLIC_KEY" \
  -H "private-secret-key: $YUNO_SECRET_KEY" \
  -H "Content-Type: application/json" \
  -d '{"account_id":"acct_123","merchant_order_id":"order-1",
       "country":"US","amount":{"currency":"USD","value":10}}'
```

</CodeCardWrapper>

<CodeCaption>
  Amounts in major units (`10` = $10.00). Headers lowercase. Full flow → [SDK Quickstart](/docs/sdks/overview/quickstart)
</CodeCaption>

<PathCards />

<AgentCallout />

<FooterLinks />
