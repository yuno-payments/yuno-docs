---
title: "Create Payment"
description: "Create a payment with Yuno. Supports various payment methods, card vaulting, and 3DS configurations."
keywords: ["browser_info", "payment_method", "vault", "checkout"]
openapi: "/openapi/payments/create-payment.json POST /payments"
---

This request creates a payment after you created a checkout session.

<Danger>
**Important**

Although certain objects are not mandatory when creating a **Payment**, the user's payment experience can be enhanced if you provide this information.
</Danger>

<Note>
**3DS Payments**

Check the [3D Secure](/docs/security-and-compliance/3d-secure) page to learn how to create payments using 3DS on Yuno.
</Note>

If you want to process payments in a Single Step (Authorization and Capture simultaneously), you need to send the `payment_method.detail.card.capture` attribute as `true`, so the payment is captured automatically.

To test all possible transaction outcomes within Yuno in **Sandbox**, please refer to the following [documentation](/docs/direct-integration-use-cases/yuno-testing-gateway#4-provide-the-payment-method-information) for **test data**.

<Danger>
**Metadata and routing logic**

If you use metadata to drive routing logic, it must also be set in the **[Checkout Session](/reference/checkout-sessions/create-checkout-session)**. Filling it only in the Payment object will not activate route logic.
</Danger>

## Request body

<ParamField body="account_id" type="string">
  The unique identifier of the account. You find this information on [the Yuno dashboard](https://dashboard.y.uno/) (UUID, 36 chars).

</ParamField>

<ParamField body="merchant_order_id" type="string">
  The unique identifier of the customer's order (MAX 255; MIN 3).

</ParamField>

<ParamField body="description" type="string">
  The description of the payment (MAX 255; MIN 3).

</ParamField>

<ParamField body="amount" type="object">
  Specifies the payment amount object, with the value and currency.

  <Expandable title="properties">

    <ParamField body="currency" type="string">
      The currency used to make the payment. For a full list of currency codes, see [Country reference](/reference/country-reference) (MAX 3; MIN 3; ISO 4217).

    </ParamField>

    <ParamField body="value" type="number">
      The payment amount (multiple of 0.0001).

    </ParamField>

    <ParamField body="currency_conversion" type="object">
      optional

      The currency conversion (DCC) object. Used when the cardholder pays in their home currency at the point of sale while the merchant settles in their own. `cardholder_currency` must differ from `amount.currency`.

      <Expandable title="properties">

        <ParamField body="id" type="string">
          optional

          The unique identifier of the [conversion rate query](/docs/currency-conversion) from Yuno. (UUID, 36 chars)

        </ParamField>

        <ParamField body="provider_currency_conversion_id" type="string">
          optional

          The unique identifier of the conversion rate query from the provider (MAX 526 ; MIN 3)

        </ParamField>

        <ParamField body="cardholder_currency" type="string">
          optional

          Currency the cardholder is charged in. ISO 4217 alpha-3 code. Must differ from `amount.currency`. This field is required if the 'currency_conversion' object is included in the request.

        </ParamField>

        <ParamField body="cardholder_amount" type="number">
          optional

          Total amount billed to the cardholder in `cardholder_currency` (multiple of 0.0001). Range: 0.0001 – 9,999,999,999. This field is required if the 'currency_conversion' object is included in the request.

        </ParamField>

        <ParamField body="cardholder_accepted" type="boolean">
          optional

          Cardholder's explicit consent to the DCC offer. `true` = accepted (charge in `cardholder_currency`); `false` = declined (payment proceeds in merchant currency).

        </ParamField>

        <ParamField body="provider_data" type="object">
          optional

          Additional data from the provider

          <Expandable title="properties">

            <ParamField body="id" type="enum">
              optional

              Identification of the provider

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="rate" type="number">
          optional

          Applied FX rate (markup included). Must be greater than 0.

        </ParamField>

        <ParamField body="rate_margin_percentage" type="number">
          optional

          Markup added over the wholesale rate, expressed as a percentage. Range: 0 – 100.

        </ParamField>

        <ParamField body="rate_source" type="string">
          optional

          Wholesale benchmark used for the quote. Required when `rate_margin_percentage > 0`. Free text, max 50 chars. Examples: `ECB`, `REUTERS`, `BLOOMBERG`, `PROVIDER_INTERNAL`.

        </ParamField>

        <ParamField body="provider" type="enum">
          optional

          DCC provider that issued the quote.

        </ParamField>

        <ParamField body="description" type="string">
          optional

          Verbatim disclosure text shown to the cardholder at the point of sale. Stored unchanged for audit. Max 4096 chars.

        </ParamField>

        <ParamField body="created_at" type="string">
          optional

          Timestamp when the DCC provider issued the quote (ISO 8601). Must be ≤ now().

        </ParamField>

        <ParamField body="expires_at" type="string">
          optional

          Timestamp when the quote expires (ISO 8601). Must be > `created_at`. The payment is rejected if `expires_at < now()` at validation time.

        </ParamField>

      </Expandable>
    </ParamField>

  </Expandable>
</ParamField>

<ParamField body="checkout" type="object">
  Specifies the checkout object. This object is not mandatory for back to back payments. Required when `workflow` is defined as `SDK_CHECKOUT` or is not sent. not required for `DIRECT` or `REDIRECT` payments.

  <Expandable title="properties">

    <ParamField body="session" type="string">
      optional

      The checkout session that has been created for the payment using the [Create Checkout Session](https://docs.y.uno/reference/create-checkout-session) endpoint (UUID, 36 chars). This field is required if the `checkout` object is included in the request.

    </ParamField>

  </Expandable>
</ParamField>

<ParamField body="additional_data" type="object">
  optional

  Specifies the additional_data object. This object is not mandatory. However, if you send this information, the payment experience will be enhanced for your user.

  <Expandable title="properties">

    <ParamField body="order" type="object">
      optional

      Specifies the order object.

      <Expandable title="properties">

        <ParamField body="shipping_amount" type="number">
          optional

          The shipping amount of the order (multiple of 0.0001). This field is for informational purposes, the shipping amount is already included in the final transaction amount and is not added separately.

        </ParamField>

        <ParamField body="fee_amount" type="number">
          optional

          The fee amount of the order (multiple of 0.0001). This field is for informational purposes, the fee amount is already included in the final transaction amount and is not added separately.

        </ParamField>

        <ParamField body="tip_amount" type="string">
          optional

          The tip amount of the order (multiple of 0.0001). This field is for informational purposes, the tip amount is already included in the final transaction amount and is not added separately.

        </ParamField>

        <ParamField body="taxes" type="array">
          optional

          The taxes of the order. This struct is for informational purposes, the taxes amount is already included in the final transaction amount and is not added separately.

          <Expandable title="properties">

            <ParamField body="type" type="enum">
              Type of the tax.

            </ParamField>

            <ParamField body="tax_base" type="number">
              optional

              The amount base to apply the tax defined.

            </ParamField>

            <ParamField body="value" type="number">
              The amount of the tax.

            </ParamField>

            <ParamField body="percentage" type="number">
              optional

              The percentage of the tax.

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="items" type="array">
          optional

          Specifies the items array of object.

          <Expandable title="properties">

            <ParamField body="id" type="string">
              The unique identifier of the item (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="name" type="string">
              The name of the item (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="quantity" type="integer">
              The quantity of the item.

            </ParamField>

            <ParamField body="unit_amount" type="number">
              The unit amount of the item (multiple of 0.0001).

            </ParamField>

            <ParamField body="category" type="enum">
              The category of the item. Check the [Item category reference](/reference/items-category-list).

            </ParamField>

            <ParamField body="brand" type="string">
              optional

              The brand of the item (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="sku_code" type="string">
              optional

              The stock keeping unit (SKU) of the item (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="manufacture_part_number" type="string">
              optional

              The manufacture part number of the item.

            </ParamField>

            <ParamField body="url" type="string">
              optional

              The item's URL available to the buyer (MAX 512; MIN 3).

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="shipping" type="object">
          optional

          The shipping details object

          <Expandable title="properties">

            <ParamField body="type" type="enum">
              optional

              The type of shippment

            </ParamField>

            <ParamField body="description" type="string">
              optional

              The shipment description

            </ParamField>

            <ParamField body="carrier" type="enum">
              optional

              Carrier used for the delivery

            </ParamField>

            <ParamField body="deliver_at" type="string">
              optional

              Estimated date of delivery (ISO 8601 MAX 27; MIN 27)

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="tickets" type="array">
          optional

          The tickets detail object

          <Expandable title="properties">

            <ParamField body="id" type="string">
              optional

              Id of the ticket (Min: 1, max:255)

            </ParamField>

            <ParamField body="name" type="string">
              optional

              Name of the ticket (Min: 1, max:255)

            </ParamField>

            <ParamField body="description" type="string">
              optional

              Description of the ticket (Min: 1, max:255)

            </ParamField>

            <ParamField body="type" type="enum">
              optional

              Type of the ticket

            </ParamField>

            <ParamField body="amount" type="object">
              optional

              Amount of the ticket

              <Expandable title="properties">

                <ParamField body="value" type="number">
                  optional

                  Amount of the payment

                </ParamField>

                <ParamField body="currency" type="string">
                  optional

                  The currency used to make the payment (ISO 4217 MAX 3; MIN 3)

                </ParamField>

              </Expandable>
            </ParamField>

            <ParamField body="event" type="object">
              optional

              The event information

              <Expandable title="properties">

                <ParamField body="id" type="string">
                  optional

                  Id of the event related to the ticket

                </ParamField>

                <ParamField body="name" type="string">
                  optional

                  Name of the event

                </ParamField>

                <ParamField body="description" type="string">
                  optional

                  Description of the event

                </ParamField>

                <ParamField body="type" type="enum">
                  optional

                  Type of the event

                </ParamField>

                <ParamField body="date" type="string">
                  optional

                  Date of the event

                </ParamField>

                <ParamField body="address" type="object">
                  optional

                  Address of the event

                  <Expandable title="properties">

                    <ParamField body="address_line_1" type="string">
                      optional

                      The primary billing address line (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="address_line_2" type="string">
                      optional

                      The secondary billing address line (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="building_number_1" type="string">
                      optional

                      The primary building number of the customer (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="building_number_2" type="string">
                      optional

                      The secondary building number of the customer (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="city" type="string">
                      optional

                      The city considered for the address (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="country" type="string">
                      optional

                      The country consider for the address (ISO 3166-1 MAX 2; MIN 2)

                    </ParamField>

                    <ParamField body="state" type="string">
                      optional

                      The state / province considered for the  address (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="zip_code" type="string">
                      optional

                      The zipcode considered for the  address (MAX 11; MIN 4)

                    </ParamField>

                    <ParamField body="neighborhood" type="string">
                      optional

                      The neighborhood address line (MAX 255; MIN 3)

                    </ParamField>

                  </Expandable>
                </ParamField>

              </Expandable>
            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="account_funding" type="object">
          optional

          The account funding transaction details object necessary for [AFTs](/docs/account-funding-transactions-afts).

          <Expandable title="properties">

            <ParamField body="sender" type="object">
              optional

              The sender object necessary for account funding transactions

              <Expandable title="properties">

                <ParamField body="first_name" type="string">
                  Sender's name. (Max: 80)

                </ParamField>

                <ParamField body="last_name" type="string">
                  optional

                  Sender's last name. (Max: 80)

                </ParamField>

                <ParamField body="national_entity" type="enum">
                  optional

                  Sender's national entity type.

                </ParamField>

                <ParamField body="legal_name" type="string">
                  optional

                  Sender's name. (Max: 80) Only necessary when national entity is ENTITY

                </ParamField>

                <ParamField body="email" type="string">
                  optional

                  The Sender's email (MAX 255; MIN 3)

                </ParamField>

                <ParamField body="country" type="string">
                  The Sender's country  (ISO 3166-1 MAX 2; MIN 2)

                </ParamField>

                <ParamField body="date_of_birth" type="string">
                  optional

                  The beneficiary's date of birth in the YYYY-MM-DD (MAX 10; MIN 10)

                </ParamField>

                <ParamField body="document" type="object">
                  optional

                  Sender's document object

                  <Expandable title="properties">

                    <ParamField body="document_number" type="string">
                      optional

                      Document number for customer (MAX 40; MIN 3). This field is required if the 'document' object is included in the request.

                    </ParamField>

                    <ParamField body="document_type" type="enum">
                      optional

                      The customer's document type (MAX 6, MIN 2; [Country reference](/reference/country-reference)).

                    </ParamField>

                  </Expandable>
                </ParamField>

                <ParamField body="phone" type="object">
                  optional

                  Sender's phone object

                  <Expandable title="properties">

                    <ParamField body="country_code" type="string">
                      optional

                      The country calling code of the customer's phone (MAX 3; MIN 1). Check the [Country reference](/reference/country-reference) to see the phone codes. This field is required if the 'phone' object is included in the request.

                    </ParamField>

                    <ParamField body="number" type="string">
                      optional

                      The customer's phone number, without the country code (MAX 32; MIN 1). This field is required if the 'phone' object is included in the request.

                    </ParamField>

                  </Expandable>
                </ParamField>

                <ParamField body="address" type="object">
                  optional

                  Sender's address object

                  <Expandable title="properties">

                    <ParamField body="address_line_1" type="string">
                      optional

                      The primary billing address line of the customer (MAX 255; MIN 1). This field is required if the 'billing_address' object is included in the request.

                    </ParamField>

                    <ParamField body="address_line_2" type="string">
                      optional

                      The secondary billing address line of the customer (MAX 255; MIN 1).

                    </ParamField>

                    <ParamField body="building_number_1" type="string">
                      optional

                      The primary building number of the customer (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="building_number_2" type="string">
                      optional

                      The secondary building number of the customer (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="city" type="string">
                      optional

                      The city considered for the billing address (MAX 255; MIN 1). This field is required if the 'billing_address' object is included in the request.

                    </ParamField>

                    <ParamField body="country" type="enum">
                      optional

                      The country considered for the billing address (MAX 2; MIN 2; [ISO 3166-1](/reference/country-reference)). This field is required if the 'billing_address' object is included in the request.

                    </ParamField>

                    <ParamField body="state" type="string">
                      optional

                      State or province code. Use ISO 3166-2 subdivision codes (https://en.wikipedia.org/wiki/ISO_3166-2).

                    </ParamField>

                    <ParamField body="zip_code" type="string">
                      optional

                      The zipcode considered for the billing address (MAX 11; MIN 3).

                    </ParamField>

                    <ParamField body="neighborhood" type="string">
                      optional

                      The neighborhood of the address line of the customer(MAX 255; MIN 1)

                    </ParamField>

                  </Expandable>
                </ParamField>

              </Expandable>
            </ParamField>

            <ParamField body="beneficiary" type="object">
              optional

              The beneficiary object necessary for account funding transactions.

              <Expandable title="properties">

                <ParamField body="first_name" type="string">
                  Beneficiary's name. (Max: 80)

                </ParamField>

                <ParamField body="last_name" type="string">
                  optional

                  Beneficiary's last name. (Max: 80)

                </ParamField>

                <ParamField body="national_entity" type="enum">
                  optional

                  Beneficiary's national entity type.

                </ParamField>

                <ParamField body="legal_name" type="string">
                  optional

                  Beneficiary's name. (Max: 80) Only necessary when national entity is ENTITY

                </ParamField>

                <ParamField body="email" type="string">
                  optional

                  The Beneficiary's email (MAX 255; MIN 3)

                </ParamField>

                <ParamField body="country" type="string">
                  The Beneficiary's country  (ISO 3166-1 MAX 2; MIN 2)

                </ParamField>

                <ParamField body="date_of_birth" type="string">
                  optional

                  The beneficiary's date of birth in the YYYY-MM-DD (MAX 10; MIN 10)

                </ParamField>

                <ParamField body="document" type="object">
                  optional

                  The beneficiary's document object.

                  <Expandable title="properties">

                    <ParamField body="document_number" type="string">
                      optional

                      Document number for customer (MAX 40; MIN 3). This field is required if the 'document' object is included in the request.

                    </ParamField>

                    <ParamField body="document_type" type="enum">
                      optional

                      The customer's document type (MAX 6, MIN 2; [Country reference](/reference/country-reference)).

                    </ParamField>

                  </Expandable>
                </ParamField>

                <ParamField body="phone" type="object">
                  optional

                  The beneficiary's phone object.

                  <Expandable title="properties">

                    <ParamField body="country_code" type="string">
                      optional

                      The country calling code of the customer's phone (MAX 3; MIN 1). Check the [Country reference](/reference/country-reference) to see the phone codes. This field is required if the 'phone' object is included in the request.

                    </ParamField>

                    <ParamField body="number" type="string">
                      optional

                      The customer's phone number, without the country code (MAX 32; MIN 1). This field is required if the 'phone' object is included in the request.

                    </ParamField>

                  </Expandable>
                </ParamField>

                <ParamField body="address" type="object">
                  optional

                  The beneficiary's address object.

                  <Expandable title="properties">

                    <ParamField body="address_line_1" type="string">
                      optional

                      The primary billing address line of the customer (MAX 255; MIN 1). This field is required if the 'billing_address' object is included in the request.

                    </ParamField>

                    <ParamField body="address_line_2" type="string">
                      optional

                      The secondary billing address line of the customer (MAX 255; MIN 1).

                    </ParamField>

                    <ParamField body="building_number_1" type="string">
                      optional

                      The primary building number of the customer (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="building_number_2" type="string">
                      optional

                      The secondary building number of the customer (MAX 255; MIN 3)

                    </ParamField>

                    <ParamField body="city" type="string">
                      optional

                      The city considered for the billing address (MAX 255; MIN 1). This field is required if the 'billing_address' object is included in the request.

                    </ParamField>

                    <ParamField body="country" type="enum">
                      optional

                      The country considered for the billing address (MAX 2; MIN 2; [ISO 3166-1](/reference/country-reference)). This field is required if the 'billing_address' object is included in the request.

                    </ParamField>

                    <ParamField body="state" type="string">
                      optional

                      State or province code. Use ISO 3166-2 subdivision codes (https://en.wikipedia.org/wiki/ISO_3166-2).

                    </ParamField>

                    <ParamField body="zip_code" type="string">
                      optional

                      The zipcode considered for the billing address (MAX 11; MIN 3).

                    </ParamField>

                    <ParamField body="neighborhood" type="string">
                      optional

                      The neighborhood of the address line of the customer(MAX 255; MIN 1)

                    </ParamField>

                  </Expandable>
                </ParamField>

              </Expandable>
            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="discounts" type="array">
          optional

          Specifies the discounts array of objects.

          <Expandable title="properties">

            <ParamField body="id" type="string">
              optional

              ID of the discount (MAX 255; MIN 1).

            </ParamField>

            <ParamField body="name" type="string">
              optional

              Name of the discount (MAX 255; MIN 1).

            </ParamField>

            <ParamField body="unit_amount" type="number">
              The unit amount of the discount (multiple of 0.0001).

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="sales_channel" type="string">
          optional

          Sales channel id of the payment. (MIN:3 y MAX:255)

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="airline" type="object">
      optional

      Specifies the airline object.

      <Expandable title="properties">

        <ParamField body="pnr" type="string">
          Passenger name record (MAX 10; MIN 1).

        </ParamField>

        <ParamField body="type" type="enum">
          optional

          Type of trip.

        </ParamField>

        <ParamField body="legs" type="array">
          optional

          Specifies the legs object.

          <Expandable title="properties">

            <ParamField body="passenger_id" type="string">
              optional

              Reference to passenger ID (MAX 128; MIN 1).

              Example: passenger_001

            </ParamField>

            <ParamField body="departure_airport" type="string">
              IATA code. See http://www.iata.org (MAX 3; MIN 3).

            </ParamField>

            <ParamField body="departure_datetime" type="string">
              The departure date and time in UTC time ([ISO 8601](https://en.wikipedia.org/wiki/ISO_8601), MAX 27; MIN 27).

            </ParamField>

            <ParamField body="departure_airport_country" type="string">
              optional

              Country of the departure airport (MAX 2; MIN 2; ISO 3166-1).

            </ParamField>

            <ParamField body="departure_airport_city" type="string">
              optional

              The city considered for the departure airport (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="departure_airport_timezone" type="string">
              Airport timezone (MAX 6; MIN 6).

            </ParamField>

            <ParamField body="arrival_airport" type="string">
              IATA airport code. See http://www.iata.org (MAX 3; MIN 3).

            </ParamField>

            <ParamField body="arrival_airport_country" type="string">
              optional

              Country of the arrival airport (MAX 2; MIN 2; ISO 3166-1).

            </ParamField>

            <ParamField body="arrival_airport_city" type="string">
              optional

              The city considered for the arrival airport (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="arrival_airport_timezone" type="string">
              optional

              Airport timezone (MAX 6; MIN 6).

            </ParamField>

            <ParamField body="arrival_datetime" type="string">
              optional

              The arrival date and time in UTC time ([ISO 8601](https://en.wikipedia.org/wiki/ISO_8601), MAX 27; MIN 27).

            </ParamField>

            <ParamField body="carrier_code" type="string">
              IATA carrier code. See http://www.iata.org (MAX 2; MIN 2).

            </ParamField>

            <ParamField body="flight_number" type="string">
              The flight number assigned by the airline carrier (MAX 5; MIN 1).

            </ParamField>

            <ParamField body="fare_basis_code" type="string">
              Code base rate provides specific information on the fare in addition to the class service, both required for booking (MAX 15; MIN 1).

            </ParamField>

            <ParamField body="fare_class_code" type="string">
              The fare class code of the airline (MAX 1; MIN 1). The values can be a letter (A-Z) but may vary depending on the airline's definition. Check the [Airline information reference](/reference/airline-information#fare-class-code).

            </ParamField>

            <ParamField body="base_fare" type="number">
              optional

              The transaction amount, excluding taxes and fees, the smallest unit of currency (multipleOf 0.0001).

            </ParamField>

            <ParamField body="base_fare_currency" type="string">
              optional

              The currency used to transaction amount (MAX 3; MIN 3).

            </ParamField>

            <ParamField body="stopover_code" type="string">
              optional

              1-letter code indicating whether the passenger is allowed to make a stopover. Only two types of characters are allowed: O: Stopover allowed (the letter 'O', not zero) / X: Stopover not allowed. (MAX 1; MIN 1).

            </ParamField>

            <ParamField body="order" type="integer">
              optional

              A running index (starts with 1), describing the order of legs in the same route.

            </ParamField>

            <ParamField body="route_order" type="integer">
              optional

              A running index (starts with 1), describing the order of routes by time. E.g: If an order contains 2 Routes: New-York->London->Paris (connection in London) New-York->London should have route_order=1, order=1; London->Paris should have route_order=1, order=2; Paris->London->New-York: Paris->London should have route_order=2, order=1; London->New-York should have route_order=2, order=2.

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="passengers" type="array">
          optional

          Specifies the passengers array of objects.

          <Expandable title="properties">

            <ParamField body="id" type="string">
              optional

              Passenger ID for identification (MAX 128; MIN 1).

              Example: passenger_001

            </ParamField>

            <ParamField body="first_name" type="string">
              optional

              The passenger first name (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="last_name" type="string">
              optional

              The passenger last name (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="middle_name" type="string">
              optional

              The passenger middle name (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="email" type="string">
              optional

              The passenger email (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="type" type="enum">
              optional

              The passenger type (MAX 1; MIN 1). Check the [Airline information reference](/reference/airline-information#passenger-type).

            </ParamField>

            <ParamField body="date_of_birth" type="string">
              optional

              The passenger date of birth in the YYYY-MM-DD (MAX 10; MIN 10).

            </ParamField>

            <ParamField body="nationality" type="enum">
              optional

              The passenger nationality (MAX 2; MIN 2; [ISO 3166-1](/reference/country-reference)).

            </ParamField>

            <ParamField body="document" type="object">
              optional

              Specifies the document object of the passenger.

              <Expandable title="properties">

                <ParamField body="document_number" type="string">
                  optional

                  Document number for passenger (MAX 40; MIN 3).

                </ParamField>

                <ParamField body="document_type" type="enum">
                  optional

                  The passenger's document type (MAX 6, MIN 2; [Country reference](/reference/country-reference)).

                </ParamField>

              </Expandable>
            </ParamField>

            <ParamField body="loyalty_number" type="string">
              optional

              Number of passenger loyalty program.

            </ParamField>

            <ParamField body="loyalty_tier" type="enum">
              optional

              Tier of passenger loyalty program. Check the [Airline information reference](/reference/airline-information#loyalty-tier).

            </ParamField>

            <ParamField body="phone" type="object">
              optional

              Specifies the phone object for the passenger.

              <Expandable title="properties">

                <ParamField body="country_code" type="string">
                  optional

                  The country calling code of the passenger's phone (MAX 3; MIN 1). Possible values: Check the [Country reference](/reference/country-reference).

                </ParamField>

                <ParamField body="number" type="string">
                  optional

                  The passenger's phone number, without the country code (MAX 32; MIN 1).

                </ParamField>

              </Expandable>
            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="tickets" type="array">
          optional

          Specifies the array of ticket objects.

          <Expandable title="properties">

            <ParamField body="passenger_id" type="string">
              optional

              Reference to passenger ID (MAX 128; MIN 1).

              Example: passenger_001

            </ParamField>

            <ParamField body="ticket_number" type="string">
              optional

              Ticket number (MAX 14; MIN 1).

            </ParamField>

            <ParamField body="e_ticket" type="boolean">
              optional

              Is this an e-ticket?

            </ParamField>

            <ParamField body="restricted" type="boolean">
              optional

              Indicates if the ticket is refunfable or not.

            </ParamField>

            <ParamField body="total_fare_amount" type="number">
              optional

              Total fare amount in the smallest unit of currency (multiple of 0.0001).

            </ParamField>

            <ParamField body="total_tax_amount" type="number">
              optional

              Total taxes amount in the smallest unit of currency (multiple of 0.0001).

            </ParamField>

            <ParamField body="total_fee_amount" type="number">
              optional

              Total fee amount in the smallest unit of currency (multiple of 0.0001).

            </ParamField>

            <ParamField body="issue" type="object">
              optional

              Specifies the issue object.

              <Expandable title="properties">

                <ParamField body="carrier_prefix_code" type="string">
                  optional

                  Issuing or Validating carrier. This is the AWB Prefix (Air waybill) 3-numeric code (MAX 3; MIN 3).

                </ParamField>

                <ParamField body="travel_agent_code" type="string">
                  optional

                  Code of the travel agent issuing the ticket (MAX 9; MIN 8).

                </ParamField>

                <ParamField body="travel_agent_name" type="string">
                  optional

                  The name under which the point of sale appears on the agency list or franchise name (MAX 32; MIN 1).

                </ParamField>

                <ParamField body="booking_system_code" type="string">
                  optional

                  Code of the booking system issuing the ticket. (MAX 255; MIN 1).

                </ParamField>

                <ParamField body="booking_system_name" type="string">
                  optional

                  Name of the booking system issuing the ticket. (MAX 255; MIN 1).

                </ParamField>

                <ParamField body="date" type="string">
                  optional

                  Ticket issuing date (MAX 27; MIN 27).

                </ParamField>

                <ParamField body="address" type="string">
                  optional

                  Address of the agent whose sold the ticket (MAX 255; MIN 3).

                </ParamField>

                <ParamField body="city" type="string">
                  optional

                  City name of the agent whose sold the ticket (MAX 255; MIN 3).

                </ParamField>

                <ParamField body="country" type="enum">
                  optional

                  Country code alpha 2 where the ticket was issued (MAX 2; MIN 2; [ISO 3166-1](/reference/country-reference)).

                </ParamField>

              </Expandable>
            </ParamField>

          </Expandable>
        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="transportations" type="array">
      optional

      Specifies the transportations array of objects.

      <Expandable title="properties">

        <ParamField body="id" type="string">
          optional

          The id of the transportation booking (MAX 255; MIN 1).

          Example: AAA0001

        </ParamField>

        <ParamField body="description" type="string">
          optional

          The description of the transportation booking (MAX 255; MIN 1).

          Example: Bus ticket

        </ParamField>

        <ParamField body="type" type="enum">
          optional

          The type of transportation booking.

          Example: ROUNDTRIP

        </ParamField>

        <ParamField body="legs" type="array">
          optional

          Specifies the legs array of objects.

          <Expandable title="properties">

            <ParamField body="passenger_id" type="string">
              optional

              Reference to passenger ID (MAX 128; MIN 1).

              Example: passenger_001

            </ParamField>

            <ParamField body="order" type="integer">
              optional

              A running index (starts with 1), describing the order of legs in the same route.

              Example: 2

            </ParamField>

            <ParamField body="route_order" type="integer">
              optional

              A running index (starts with 1), describing the order of routes by time.

              Example: 1

            </ParamField>

            <ParamField body="departure_country" type="string">
              optional

              Country of the departure (MAX 2; MIN 2; ISO 3166-1).

              Example: CO

            </ParamField>

            <ParamField body="departure_city" type="string">
              optional

              The city of the departure (MAX 255; MIN 1).

              Example: Bogota

            </ParamField>

            <ParamField body="departure_timezone" type="string">
              optional

              Departure timezone (MAX 6; MIN 6).

              Example: -03:00

            </ParamField>

            <ParamField body="departure_datetime" type="string">
              optional

              The departure date and time in local time at the departure.

              Example: 2022-05-09T20:46:54.786342Z

            </ParamField>

            <ParamField body="departure_reference" type="string">
              optional

              The reference of the departure (MAX 255; MIN 1).

              Example: AAA001-2

            </ParamField>

            <ParamField body="arrival_country" type="string">
              optional

              Country of the arrival (MAX 2; MIN 2; ISO 3166-1).

              Example: CO

            </ParamField>

            <ParamField body="arrival_city" type="string">
              optional

              The city of the arrival (MAX 255; MIN 1).

              Example: Medellin

            </ParamField>

            <ParamField body="arrival_timezone" type="string">
              optional

              Arrival timezone (MAX 6; MIN 6).

              Example: -03:00

            </ParamField>

            <ParamField body="arrival_datetime" type="string">
              optional

              The arrival date and time in local time at the arrival.

              Example: 2022-05-09T24:46:54.786342Z

            </ParamField>

            <ParamField body="arrival_reference" type="string">
              optional

              The reference of the arrival (MAX 255; MIN 1).

              Example: AAA001-4

            </ParamField>

            <ParamField body="transport" type="object">
              optional

              Specifies the transport object.

              <Expandable title="properties">

                <ParamField body="id" type="string">
                  optional

                  The id of the transportation vehicle (MAX 255; MIN 1).

                  Example: ASONDAKS-1234

                </ParamField>

              </Expandable>
            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="transport" type="object">
          optional

          Specifies the transport object.

          <Expandable title="properties">

            <ParamField body="id" type="string">
              optional

              The id of the transportation vehicle (MAX 255; MIN 1).

              Example: ASONDAKS-1234

            </ParamField>

            <ParamField body="type" type="enum">
              optional

              The type of transportation vehicle.

              Example: BUS

            </ParamField>

            <ParamField body="description" type="string">
              optional

              The description of the transportation vehicle (MAX 255; MIN 1).

              Example: BUS 2022

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="passengers" type="array">
          optional

          Specifies the passengers array of objects.

          <Expandable title="properties">

            <ParamField body="id" type="string">
              optional

              Passenger ID for identification (MAX 128; MIN 1).

              Example: passenger_001

            </ParamField>

            <ParamField body="first_name" type="string">
              optional

              The passenger first name (MAX 255; MIN 3).

              Example: John

            </ParamField>

            <ParamField body="last_name" type="string">
              optional

              The passenger last name (MAX 255; MIN 3).

              Example: Doe

            </ParamField>

            <ParamField body="middle_name" type="string">
              optional

              The passenger middle name (MAX 255; MIN 3).

              Example: A

            </ParamField>

            <ParamField body="email" type="string">
              optional

              The passenger email (MAX 255; MIN 3).

              Example: john.doe@example.com

            </ParamField>

            <ParamField body="type" type="string">
              optional

              The passenger type (MAX 1; MIN 1). Check the Airline information reference.

              Example: A

            </ParamField>

            <ParamField body="date_of_birth" type="string">
              optional

              The passenger date of birth in the YYYY-MM-DD (MAX 10; MIN 10).

              Example: 1990-01-01

            </ParamField>

            <ParamField body="nationality" type="string">
              optional

              The passenger nationality (MAX 2; MIN 2; ISO 3166-1).

              Example: CO

            </ParamField>

            <ParamField body="document" type="object">
              optional

              Specifies the document object of the passenger.

              <Expandable title="properties">

                <ParamField body="document_number" type="string">
                  optional

                  Document number for passenger (MAX 40; MIN 3).

                  Example: 123456789

                </ParamField>

                <ParamField body="document_type" type="string">
                  optional

                  The passenger's document type (MAX 6, MIN 2; [Document Type List](https://docs.y.uno/reference/country-reference)).

                  Example: CC

                </ParamField>

              </Expandable>
            </ParamField>

            <ParamField body="loyalty_number" type="string">
              optional

              Number of passenger loyalty program.

              Example: L123456

            </ParamField>

            <ParamField body="loyalty_tier" type="string">
              optional

              Tier of passenger loyalty program. Check the Airline information reference.

              Example: GOLD

            </ParamField>

            <ParamField body="phone" type="object">
              optional

              Specifies the phone object for the passenger.

              <Expandable title="properties">

                <ParamField body="country_code" type="string">
                  optional

                  The country calling code of the passenger's phone (MAX 3; MIN 1).

                  Example: 57

                </ParamField>

                <ParamField body="number" type="string">
                  optional

                  The passenger's phone number, without the country code (MAX 32; MIN 1).

                  Example: 3001234567

                </ParamField>

              </Expandable>
            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="tickets" type="array">
          optional

          Specifies the array of ticket objects.

          <Expandable title="properties">

            <ParamField body="passenger_id" type="string">
              optional

              Reference to passenger ID (MAX 128; MIN 1).

              Example: passenger_001

            </ParamField>

            <ParamField body="ticket_number" type="string">
              optional

              Ticket number (MAX 14; MIN 1).

              Example: 12345678901234

            </ParamField>

            <ParamField body="e_ticket" type="boolean">
              optional

              Is this an e-ticket?

              Example: True

            </ParamField>

            <ParamField body="restricted" type="boolean">
              optional

              Indicates if the ticket is refundable or not.

            </ParamField>

            <ParamField body="total_fare_amount" type="number">
              optional

              Total fare amount in the smallest unit of currency (multiple of 0.0001).

              Example: 50000

            </ParamField>

            <ParamField body="total_tax_amount" type="number">
              optional

              Total taxes amount in the smallest unit of currency (multiple of 0.0001).

              Example: 5000

            </ParamField>

            <ParamField body="total_fee_amount" type="number">
              optional

              Total fee amount in the smallest unit of currency (multiple of 0.0001).

              Example: 1000

            </ParamField>

          </Expandable>
        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="seller_details" type="object">
      Specifies the seller's details object.

      <Expandable title="properties">

        <ParamField body="name" type="string">
          optional

          The seller's legal name (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="email" type="string">
          optional

          The seller's e-mail (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="reference" type="string">
          optional

          The seller's identification code (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="website" type="string">
          optional

          The seller's website URL (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="industry" type="enum">
          optional

          The seller's industry (MAX 235; MIN 1) For more information access the [Industry category reference page](/reference/industry-category-list).

        </ParamField>

        <ParamField body="merchant_category_code" type="string">
          optional

          MCC - The merchant category code (MAX 235; MIN 1).

        </ParamField>

        <ParamField body="country" type="enum">
          optional

          The seller's country (MAX 255; MIN 2; [ISO 3166-1](/reference/country-reference)).

        </ParamField>

        <ParamField body="document" type="object">
          optional

          Specifies the document object of the seller.

          <Expandable title="properties">

            <ParamField body="document_number" type="string">
              optional

              The seller's document number (MAX 40; MIN 3).

            </ParamField>

            <ParamField body="document_type" type="enum">
              optional

              The seller's document type (MAX 6, MIN 2; [Document Type List](https://docs.y.uno/reference/country-reference)).

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="phone" type="object">
          optional

          Specifies the seller's phone number object.

          <Expandable title="properties">

            <ParamField body="country_code" type="string">
              optional

              The country calling code of the seller's phone (MAX 3; MIN 1). Check the [Country reference](/reference/country-reference) to see the phone codes.

            </ParamField>

            <ParamField body="number" type="string">
              optional

              The seller's phone number, without the country code (MAX 32; MIN 1).

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="address" type="object">
          Specifies the seller's address object.

          <Expandable title="properties">

            <ParamField body="address_line_1" type="string">
              The primary address line of the seller (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="address_line_2" type="string">
              optional

              The secondary billing address line of the seller (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="building_number_1" type="string">
              optional

              The primary building number of the customer (MAX 255; MIN 3)

            </ParamField>

            <ParamField body="building_number_2" type="string">
              optional

              The secondary building number of the customer (MAX 255; MIN 3)

            </ParamField>

            <ParamField body="city" type="string">
              The city considered for the seller's address (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="country" type="enum">
              The country considered for the seller's address (MAX 2; MIN 2; [ISO 3166-1](/reference/country-reference)).

            </ParamField>

            <ParamField body="state" type="string">
              optional

              The state considered for the seller's address (MAX 255; MIN 1).

            </ParamField>

            <ParamField body="zip_code" type="string">
              optional

              The zipcode considered for the seller's address (MAX 11; MIN 4).

            </ParamField>

          </Expandable>
        </ParamField>

      </Expandable>
    </ParamField>

  </Expandable>
</ParamField>

<ParamField body="country" type="enum">
  Country where the transaction must be processed (MAX 2; MIN 2; [ISO 3166-1](/reference/country-reference)).

</ParamField>

<ParamField body="merchant_reference" type="string">
  optional

  Identification of the payment transaction defined by the merchant (MAX 255; MIN 3). Optional to complement the merchant_order_id.

</ParamField>

<ParamField body="customer_payer" type="object">
  optional

  Specifies customer object for payments.

  <Expandable title="properties">

    <ParamField body="id" type="string">
      optional

      The unique identifier of the customer (UUID, 36 chars). This `id` is returned in Yuno's [Create Customer](/reference/create-customer) endpoint. This object is not mandatory for DIRECT payments. If the `id` of a customer is sent, it is not necessary to complete the rest of the `customer_payer` object, the information of the previously created customer will be taken into account for the payment. <br/> Example: `be8a1640-43d2-4a75-9694-c6468d82cba3`.

    </ParamField>

    <ParamField body="merchant_customer_id" type="string">
      optional

      The unique identifier for a customer in the merchant's system, assigned by the merchant (MAX 255; MIN 1).

    </ParamField>

    <ParamField body="merchant_customer_created_at" type="string">
      optional

      Customer´s registration date on the merchants platform (ISO 8601 MAX 27; MIN 27)

    </ParamField>

    <ParamField body="merchant_customer_validations" type="object">
      optional

      Fraud validations for the customer payer

      <Expandable title="properties">

        <ParamField body="account_is_verified" type="boolean">
          optional

          If the customer's account has been validated before on the merchant's side. True by default

        </ParamField>

        <ParamField body="email_is_verified" type="boolean">
          optional

          If the customer's email has been validated before on the merchant's side. True by default

        </ParamField>

        <ParamField body="phone_is_verified" type="boolean">
          optional

          If the customer's phone has been validated before on the merchant's side. True by default

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="first_name" type="string">
      optional

      The customer's first name (MAX 255; MIN 1).

    </ParamField>

    <ParamField body="last_name" type="string">
      optional

      The customer's last name (MAX 255; MIN 1).

    </ParamField>

    <ParamField body="gender" type="enum">
      optional

      The customer's gender (MAX 2; MIN 1; (M=Male/F=Female/NB=Non Binary/NA=Not applicable/NK=Not Known/U=Undefined)).

    </ParamField>

    <ParamField body="date_of_birth" type="string">
      optional

      The customer's date of birth in the YYYY-MM-DD format (Length: 10).

    </ParamField>

    <ParamField body="email" type="string">
      optional

      The customer's e-mail (MAX 255; MIN 1).

    </ParamField>

    <ParamField body="nationality" type="enum">
      optional

      The customer's nationality (MAX 2; MIN 2; [ISO 3166-1](/reference/country-reference)).

    </ParamField>

    <ParamField body="ip_address" type="string">
      optional

      The customer's IP address (MAX 45; MIN 1).

    </ParamField>

    <ParamField body="device_fingerprints" type="array">
      optional

      The customer's [device fingerprints](/docs/direct-flow#important-add-ons) (MAX 4000; MIN 1). In case you are using a DIRECT integration and want to support fraud providers in the payment flow, you can use this object to specify the necessary information. For integrations using Yuno checkout the value is obtained automatically, do not send this field.

      <Expandable title="properties">

        <ParamField body="provider_id" type="enum">
          The fraud screening provider id

        </ParamField>

        <ParamField body="id" type="string">
          The device fingerprint associated to the provider

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="browser_info" type="object">
      optional

      Specifies the browser_info object.

      <Expandable title="properties">

        <ParamField body="user_agent" type="string">
          optional

          The user agent value of the customer's browser (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="accept_header" type="string">
          optional

          The accept header value of the customer's browser.

        </ParamField>

        <ParamField body="platform" type="enum">
          optional

          The platform from which the customer is making the payment.

        </ParamField>

        <ParamField body="color_depth" type="string">
          optional

          The color depth of the customer's browser in bits per pixel. This should be obtained by using the browser's screen.colorDepth property. Accepted values: 1, 4, 8, 15, 16, 24, 30, 32 or 48 bit color depth. (MAX 5; MIN 1).

        </ParamField>

        <ParamField body="screen_height" type="string">
          optional

          The total height of the customer's device screen in pixels. (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="screen_width" type="string">
          optional

          The total width of the customer's device screen in pixels. (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="javascript_enabled" type="boolean">
          optional

          Indicates if Javascript is enabled or not in the device.

        </ParamField>

        <ParamField body="language" type="string">
          optional

          The navigator.language value of the customer's browser (as defined in IETF BCP 47) (MAX 5; MIN 1).

        </ParamField>

        <ParamField body="accept_browser" type="string">
          optional

          The accept browser value of the customer's browser

        </ParamField>

        <ParamField body="accept_content" type="string">
          optional

          The accept content value of the customer's browser

        </ParamField>

        <ParamField body="java_enabled" type="boolean">
          optional

          Indicates if Java is enabled or not in the device

        </ParamField>

        <ParamField body="browser_time_difference" type="string">
          optional

          Indicates the browser time difference

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="document" type="object">
      optional

      Specifies the customer's document object, including its number and type.

      <Expandable title="properties">

        <ParamField body="document_number" type="string">
          optional

          Document number for customer (MAX 40; MIN 3). This field is required if the 'document' object is included in the request.

        </ParamField>

        <ParamField body="document_type" type="enum">
          optional

          The customer's document type (MAX 6, MIN 2; [Country reference](/reference/country-reference)).

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="billing_address" type="object">
      optional

      Specifies the customer's billing address object.

      <Expandable title="properties">

        <ParamField body="address_line_1" type="string">
          optional

          The primary billing address line of the customer (MAX 255; MIN 3). This field is required if the 'billing_address' object is included in the request.

        </ParamField>

        <ParamField body="address_line_2" type="string">
          optional

          The secondary billing address line of the customer (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="building_number_1" type="string">
          optional

          The primary building number of the customer (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="building_number_2" type="string">
          optional

          The secondary building number of the customer (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="city" type="string">
          optional

          The city considered for the billing address (MAX 255; MIN 3). This field is required if the 'billing_address' object is included in the request.

        </ParamField>

        <ParamField body="country" type="enum">
          optional

          The country considered for the billing address (MAX 2; MIN 2; [ISO 3166-1](/reference/country-reference)). This field is required if the 'billing_address' object is included in the request.

        </ParamField>

        <ParamField body="state" type="string">
          optional

          State or province code. Use ISO 3166-2 subdivision codes (https://en.wikipedia.org/wiki/ISO_3166-2).

        </ParamField>

        <ParamField body="zip_code" type="string">
          optional

          The zip code considered for the billing address (MAX 10; MIN 5).

        </ParamField>

        <ParamField body="neighborhood" type="string">
          optional

          The neighborhood of the address line of the customer (MAX 255; MIN 2)

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="shipping_address" type="object">
      optional

      Specifies the customer's shipping address object.

      <Expandable title="properties">

        <ParamField body="address_line_1" type="string">
          optional

          The primary shipping address line of the customer (MAX 255; MIN 3). This field is required if the 'shipping_address' object is included in the request.

        </ParamField>

        <ParamField body="address_line_2" type="string">
          optional

          The secondary shipping address line of the customer (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="building_number_1" type="string">
          optional

          The primary building number of the customer (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="building_number_2" type="string">
          optional

          The secondary building number of the customer (MAX 255; MIN 3).

        </ParamField>

        <ParamField body="city" type="string">
          optional

          The city considered for the shipping address (MAX 255; MIN 3). This field is required if the 'shipping_address' object is included in the request.

        </ParamField>

        <ParamField body="country" type="enum">
          optional

          The country considered for the shipping address (MAX 2; MIN 2; [ISO 3166-1](/reference/country-reference)). **[Required]**.

        </ParamField>

        <ParamField body="state" type="string">
          optional

          State or province code. Use ISO 3166-2 subdivision codes (https://en.wikipedia.org/wiki/ISO_3166-2).

        </ParamField>

        <ParamField body="zip_code" type="string">
          optional

          The zip code considered for the shipping address (MAX 10; MIN 5).

        </ParamField>

        <ParamField body="neighborhood" type="string">
          optional

          The neighborhood of the address line of the customer (MAX 255; MIN 2)

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="phone" type="object">
      optional

      Specifies the customer's phone number object.

      <Expandable title="properties">

        <ParamField body="country_code" type="string">
          optional

          The country calling code of the customer's phone (MAX 3; MIN 1). Check the [Country reference](/reference/country-reference) to see the phone codes. This field is required if the 'phone' object is included in the request.

        </ParamField>

        <ParamField body="number" type="string">
          optional

          The customer's phone number, without the country code (MAX 32; MIN 1). This field is required if the 'phone' object is included in the request.

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="geolocation" type="object">
      optional

      Customer's geolocation.

      <Expandable title="properties">

        <ParamField body="latitude" type="string">
          optional

          Angular distance of a location on the earth south or north of the equator (MIN: 1, MAX:11)

        </ParamField>

        <ParamField body="longitude" type="string">
          optional

          Angular measurement of the distance of a location on the earth east or west of the Greenwich observatory. (MIN: 1, MAX:11)

        </ParamField>

      </Expandable>
    </ParamField>

  </Expandable>
</ParamField>

<ParamField body="workflow" type="enum">
  optional

  The payment workflow. Indicates whether the integration will use Yuno´s SDK or will be a back to back connection. [Guides](/docs/sdks/overview/quickstart) <ul> <li>`SDK_CHECKOUT`: [Recommended] - Use Yuno's SDK. </li> <li>`DIRECT`: Back to back integration with provider info for custom payment experience. </li> <li>`REDIRECT`: Back to back integration with provider redirection. </li> </ul>

</ParamField>

<ParamField body="payment_method" type="object">
  The payment method object.

  <Expandable title="properties">

    <ParamField body="token" type="string">
      optional

      The one time use payment method token **provided by Yuno sdk**. If a payment is created using a token, it is not necessary to send a vaulted_token as well, it can be defined as null. Not necessary for back to back payments (MAX: 64; MIN: 36).

    </ParamField>

    <ParamField body="vaulted_token" type="string">
      optional

      The vaulted token for a **previously stored payment_method**. If a payment is created using a vaulted_token, it is not necessary to send a token as well, it can be defined as null (MAX: 64; MIN: 36).

    </ParamField>

    <ParamField body="type" type="enum">
      optional

      Type of the payment method. Only required for `DIRECT` or `REDIRECT` workflow. Required when integrating wallets, even when using the SDK.

    </ParamField>

    <ParamField body="detail" type="object">
      optional

      Specifies the payment method detail object.

      <Expandable title="properties">

        <ParamField body="card" type="object">
          optional

          Specifies the card object.

          <Expandable title="properties">

            <ParamField body="capture" type="boolean">
              optional

              Determines whether the card payment is captured immediately (`TRUE` = purchase) or only authorized (`FALSE` = requires capture or cancel).

            </ParamField>

            <ParamField body="delayed_capture_settings" type="object">
              optional

              Configuration for automatically capturing the full authorized amount after a delay. Only valid when capture = `FALSE`.

              <Expandable title="properties">

                <ParamField body="delay" type="string">
                  optional

                  Delay before Yuno performs the capture (ISO 8601 format).

                  Example: P20D

                </ParamField>

                <ParamField body="simplified_mode" type="boolean">
                  optional

                  If true, Yuno retries the capture if it fails.

                </ParamField>

              </Expandable>
            </ParamField>

            <ParamField body="delayed_cancel_settings" type="object">
              optional

              Configuration for automatically canceling the authorization after a delay. Only valid when capture = `FALSE`.

              <Expandable title="properties">

                <ParamField body="delay" type="string">
                  optional

                  Delay before Yuno performs the cancel (ISO 8601 format).

                  Example: P20D

                </ParamField>

                <ParamField body="simplified_mode" type="boolean">
                  optional

                  If true, Yuno retries the capture if it fails.

                </ParamField>

              </Expandable>
            </ParamField>

            <ParamField body="installments" type="integer">
              optional

              The card installments.

            </ParamField>

            <ParamField body="first_installment_deferral" type="integer">
              optional

              Number of months to be waited to debit the first installment.

            </ParamField>

            <ParamField body="soft_descriptor" type="string">
              optional

              The descriptor passed per transaction. It will be presented on the customer's bank statement (MAX 255).

            </ParamField>

            <ParamField body="card_data" type="object">
              optional

              Specifies the card_data object.

              <Expandable title="properties">

                <ParamField body="number" type="string">
                  Card's number without any separators. (MAX 19; MIN 8) - only available for PCI certified merchants.

                </ParamField>

                <ParamField body="expiration_month" type="integer">
                  optional

                  Card's expiration month - MM (MAX 2; MIN 2) - only available for PCI certified merchants.

                </ParamField>

                <ParamField body="expiration_year" type="integer">
                  optional

                  Card's expiration year - YYYY (MAX 4; MIN 2) - only available for PCI certified merchants.

                </ParamField>

                <ParamField body="security_code" type="string">
                  optional

                  Card's security code (MAX 4; MIN 3) - only available for PCI certified merchants.

                </ParamField>

                <ParamField body="holder_name" type="string">
                  Cardholder's full name as it appears on the card (MAX 26; MIN 3) - only available for PCI certified merchants.

                </ParamField>

                <ParamField body="type" type="enum">
                  optional

                  The card type. For dual cards (like in Brazil), you can specify the type of card that is being processed so we can inform the providers.

                </ParamField>

              </Expandable>
            </ParamField>

            <ParamField body="network_token" type="object">
              optional

              The network token object. Only available for merchants that handle the network token generation/management from their side. [Direct Network token guide](https://docs.y.uno/docs/network-tokens#2--use-your-existing-network-tokens)

              <Expandable title="properties">

                <ParamField body="token_data" type="object">
                  optional

                  Specifies the token_data object.

                  <Expandable title="properties">

                    <ParamField body="number" type="string">
                      [Mandatory] - Token's number without any separators. (MAX 19; MIN 8) - only available for PCI certified merchants

                    </ParamField>

                    <ParamField body="holder_name" type="string">
                      optional

                      Cardholder's full name as it appears on the Token (MAX 26; MIN 3)

                    </ParamField>

                    <ParamField body="expiration_month" type="string">
                      Token's expiration month - MM (MAX 2; MIN 2)

                    </ParamField>

                    <ParamField body="expiration_year" type="string">
                      Token's expiration year - YYYY (MAX 4; MIN 4) - only available for PCI certified merchants

                    </ParamField>

                    <ParamField body="cryptogram" type="string">
                      [Mandatory] - The unique cryptogram generated by the issuer for the network token in use in the transaction.

                    </ParamField>

                    <ParamField body="electronic_commerce_indicator" type="string">
                      optional

                      [Only required for certain providers] - In case the token has been authenticated by Mastercard the field should be set to 02. For Visa or not authenticated tokens, is not necessary to send the field.

                    </ParamField>

                    <ParamField body="token_requestor_id" type="string">
                      optional

                      [Only required for certain providers] - Token requestor ID of the merchant

                    </ParamField>

                  </Expandable>
                </ParamField>

              </Expandable>
            </ParamField>

            <ParamField body="three_d_secure" type="object">
              optional

              3DS secure information

              <Expandable title="properties">

                <ParamField body="three_d_secure_setup_id" type="string">
                  optional

                  The identifier returned by [POST /v1/three-d-secure/setups](/reference/three-d-secure-setups/create-three-d-secure-setup). Required when using 3D Secure 2 (3DS2) authentication in a server-to-server integration where you collect browser and device data on your side.

                </ParamField>

                <ParamField body="version" type="enum">
                  optional

                  [Optional for 3DS integration without Yuno's JS] - Refers to the protocol version of the EMV 3-D Secure specification used. 1.0, 2.0, 2.1.0, 2.2.0, 2.2.1, 2.3.0 y 2.3.1

                </ParamField>

                <ParamField body="electronic_commerce_indicator" type="string">
                  optional

                  [Optional for 3DS integration without Yuno's JS] - This field must be completed with the result of the ECI field provided by the 3d Secure service. The Electronic Commerce Indicator (ECI) informs the card issuer if the transaction was protected by a security protocol like VbV or MCSC. It is mandated by Visa and MasterCard that all 3-D Secure transactions have this value populated in the authorization request. (Min: 0, max: 2).

                </ParamField>

                <ParamField body="cryptogram" type="string">
                  optional

                  [Optional for 3DS integration without Yuno's JS] - This field must be completed with the result of the cryptogram field provided by the 3DSecure service. In Visa transactions, it represents the Cardholder Authentication Verification Value (CAVV), a cryptographic value generated by the Issuer as evidence of payment authentication during online purchase to qualify for chargeback protection. MasterCard transactions have a similar value called Accountholder Authentication Value (AAV) or the Universal Cardholder Authentication Field (UCAF). When submitting a transaction for authorization, the merchant must include the CAVV or AAV/UCAF to demonstrate that the cardholder has been authenticated. It is typically base64-encoded. (Min: 0, Max: 40).

                </ParamField>

                <ParamField body="transaction_id" type="string">
                  optional

                  [Optional for 3DS integration without Yuno's JS] - For 3DS v1: This is the Unique Transaction Identifier. It is automatically generated by the MPI. It is typically 28 bytes in length and base64-encoded. Is commonly referred to as XID. (Min: 0, Max: 40). For 3DS v2: Universally unique transaction identifier assigned by the DS to identify a single transaction. (Min: 36, Max:36).

                </ParamField>

                <ParamField body="directory_server_transaction_id" type="string">
                  optional

                  [Optional for 3DS integration without Yuno's JS] - Transaction ID generated by the Mastercard directory server during authentication (MAX 255; MIN 3).

                </ParamField>

              </Expandable>
            </ParamField>

            <ParamField body="verify" type="boolean">
              optional

              Using  amount = 0 and verify = true, you can verify the user's card without authorizing a real amount. If the field is not sent, we will take it as false.

            </ParamField>

            <ParamField body="acquirer_reference_number" type="string">
              optional

              Unique identifier assigned by the card acquirer to track transactions through the network (MAX; 30).

            </ParamField>

            <ParamField body="stored_credentials" type="object">
              optional

              Indicates the processing type of the transaction. Refer to the <a href="/docs/payment-features/stored-credentials#create-a-payment-with-processing-type" class="link_stored_credential">Stored credentials</a> documentation for more information.

              <Expandable title="properties">

                <ParamField body="reason" type="enum">
                  optional

                  Indicates the processing type of the transaction.

                </ParamField>

                <ParamField body="usage" type="enum">
                  optional

                  This field Iets you indicate if this is the first time the vaulted_token/network_token is used for a payment.

                </ParamField>

                <ParamField body="subscription_agreement_id" type="string">
                  optional

                  This field lets you indicate the identification of the agreement with the customer for a subscription. Mainly for MX.(MAX 255; MIN 3). [Stored credentials](/docs/payment-features/stored-credentials)

                </ParamField>

                <ParamField body="network_transaction_id" type="string">
                  optional

                  Unique identifier assigned to a transaction by the card network. It is used to track and reference specific transactions, particularly in recurring payment scenarios, ensuring consistency and traceability across the payment lifecycle. (MAX 255; MIN 3). [Stored credentials](/docs/payment-features/stored-credentials)

                </ParamField>

              </Expandable>
            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="ticket" type="object">
          optional

          Specifies the ticket object

          <Expandable title="properties">

            <ParamField body="benefit_type" type="enum">
              optional

              Specifies the user's benefit type for Pluxee Chile.

            </ParamField>

            <ParamField body="expires_at" type="string">
              optional

              Payment methods expiration date. "yyyy-MM-dd HH:mm:ss.SSSz". Only available when the provider associated to the payment supports it. Please feel free to reach out to your technical account manager for further questions.

            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="wallet" type="object">
          optional

          Specifies the wallet object

          <Expandable title="properties">

            <ParamField body="payment_token" type="string">
              optional

              Complete wallet response payload as a JSON string, exactly as returned by the wallet provider

            </ParamField>

            <ParamField body="expires_at" type="string">
              optional

              Payment methods expiration date. "yyyy-MM-dd HH:mm:ss.SSSz". Only available when the provider associated to the payment supports it. Please feel free to reach out to your technical account manager for further questions.

            </ParamField>

            <ParamField body="capture" type="boolean">
              optional

              Indicates whether the wallet payment should be captured immediately (true) or only authorized for later capture (false)

            </ParamField>

            <ParamField body="soft_descriptor" type="string">
              optional

              The descriptor passed per transaction. It will be presented on the customer's bank statement (MAX 255).

            </ParamField>

            <ParamField body="domain_url" type="string">
              optional

              Registered domain used during the wallet flow. (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="verify" type="boolean">
              optional

              Enables card verification flow through the wallet. When true, a verification transaction (e.g., zero-dollar auth) is performed instead of a regular payment.

            </ParamField>

            <ParamField body="stored_credentials" type="object">
              optional

              Indicates the processing type of the transaction. Refer to the <a href="/docs/payment-features/stored-credentials#create-a-payment-with-processing-type" class="link_stored_credential">Stored credentials</a> documentation for more information.

              <Expandable title="properties">

                <ParamField body="reason" type="enum">
                  optional

                  Indicates the processing type of the transaction.

                </ParamField>

                <ParamField body="usage" type="enum">
                  optional

                  This field Iets you indicate if this is the first time the vaulted_token/network_token is used for a payment.

                </ParamField>

                <ParamField body="subscription_agreement_id" type="string">
                  optional

                  This field lets you indicate the identification of the agreement with the customer for a subscription. Mainly for MX.(MAX 255; MIN 3). [Stored credentials](/docs/payment-features/stored-credentials)

                </ParamField>

                <ParamField body="network_transaction_id" type="string">
                  optional

                  Unique identifier assigned to a transaction by the card network. It is used to track and reference specific transactions, particularly in recurring payment scenarios, ensuring consistency and traceability across the payment lifecycle. (MAX 255; MIN 3). [Stored credentials](/docs/payment-features/stored-credentials)

                </ParamField>

                <ParamField body="additional_data" type="object">
                  optional

                  This structure is used to send additional information about recurrence. It's optional, and few providers require it (e.g. Mercado Pago). Contact your TAM to confirm if it's needed for your integration.

                  <Expandable title="properties">

                    <ParamField body="billing_cycles" type="object">
                      optional

                      Specifies the `billing_cycles` object. Defines the number of charges associated to the subscription. If using this object, you must enter data on all fields.

                      <Expandable title="properties">

                        <ParamField body="current" type="integer">
                          optional

                          Example: 1

                        </ParamField>

                        <ParamField body="total" type="integer">
                          optional

                          The total number of billing cycles for the subscription (MIN 1).

                        </ParamField>

                      </Expandable>
                    </ParamField>

                    <ParamField body="frequency" type="object">
                      optional

                      Specifies the frequency type (e.g., DAY, WEEK, MONTH, YEAR) and value. Enter data on all fields if using this object.

                      <Expandable title="properties">

                        <ParamField body="type" type="string">
                          optional

                          Specifies the frequency type (e.g., DAY, WEEK, MONTH, YEAR).

                          Example: MONTH

                        </ParamField>

                        <ParamField body="value" type="string">
                          optional

                          Specifies the frequency value.

                        </ParamField>

                      </Expandable>
                    </ParamField>

                    <ParamField body="availability" type="object">
                      optional

                      Specifies the `availability` object. Defines a date interval based on starting and ending dates when the subscription is available to use.

                      <Expandable title="properties">

                        <ParamField body="start_at" type="string">
                          optional

                          The start date that the subscription plan will be available to use. If not set, the subscription is available from the moment it is created. `start_at` becomes mandatory when using `finish_at`.

                        </ParamField>

                        <ParamField body="finish_at" type="string">
                          optional

                          The end date until the subscription plan will be available to use. If not defined, the subscription does not have an expiration date. `finish_at` becomes mandatory when using `start_at`.

                        </ParamField>

                      </Expandable>
                    </ParamField>

                  </Expandable>
                </ParamField>

              </Expandable>
            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="bank_transfer" type="object">
          optional

          Specifies the bank transfer object.

          <Expandable title="properties">

            <ParamField body="soft_descriptor" type="string">
              optional

              Human-readable reference associated with the bank transfer. Commonly used for reconciliation and operational purposes (MAX 255; MIN 1).

            </ParamField>

            <ParamField body="expires_at" type="string">
              optional

              Payment methods expiration date. "yyyy-MM-dd HH:mm:ss.SSSz". Only available when the provider associated to the payment supports it. Please feel free to reach out to your technical account manager for further questions.

            </ParamField>

            <ParamField body="account_type" type="enum">
              optional

              Type of the bank account (MAX 255; MIN 3). CHECKINGS/SAVINGS

            </ParamField>

            <ParamField body="bank_account" type="string">
              optional

              The number of the bank account (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="bank_id" type="string">
              optional

              Identifier of the bank selected for the transfer when the provider requires choosing one.

            </ParamField>

            <ParamField body="routing_number" type="string">
              optional

              The routing_number of the bank account (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="code" type="string">
              optional

              The beneficiary's specific financial institution code (MAX 8).

            </ParamField>

            <ParamField body="branch" type="string">
              optional

              The beneficiary's specific financial institution branch (MAX 8).

            </ParamField>

            <ParamField body="branch_digit" type="string">
              optional

              The beneficiary's specific financial institution branch digit (MAX 2).

            </ParamField>

            <ParamField body="beneficiary_name" type="string">
              optional

              Name of the account holder (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="beneficiary_type" type="enum">
              optional

              Type of the beneficiary (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="third_party_token_id" type="string">
              optional

              Tokenized ID of the destination account (MAX 255; MIN 3).

            </ParamField>

            <ParamField body="account_holder_type" type="enum">
              optional

              Bank account holder type (for ACH payments).

            </ParamField>

            <ParamField body="stored_credentials" type="object">
              optional

              Indicates the processing type of the transaction. Refer to the [stored credentials](/docs/payment-features/stored-credentials#create-a-payment-with-processing-type) documentation for more information.

              <Expandable title="properties">

                <ParamField body="reason" type="enum">
                  optional

                  Indicates the processing type of the transaction.

                </ParamField>

                <ParamField body="usage" type="enum">
                  optional

                  This field Iets you indicate if this is the first time the vaulted_token/network_token is used for a payment.

                </ParamField>

                <ParamField body="subscription_agreement_id" type="string">
                  optional

                  This field lets you indicate the identification of the agreement with the customer for a subscription (MAX 255; MIN 3). See [Stored credentials](/docs/payment-features/stored-credentials) for more info.

                </ParamField>

                <ParamField body="additional_data" type="object">
                  optional

                  This object is used to send additional information about recurrence.

                  <Expandable title="properties">

                    <ParamField body="billing_cycles" type="object">
                      optional

                      Defines the number of charges associated to the subscription. If using this object, you must enter data on all fields.

                      <Expandable title="properties">

                        <ParamField body="current" type="string">
                          optional

                          Current billing cycles for the subscription.

                        </ParamField>

                        <ParamField body="total" type="string">
                          optional

                          The total number of billing cycles for the subscription.

                        </ParamField>

                      </Expandable>
                    </ParamField>

                    <ParamField body="frequency" type="object">
                      optional

                      Specifies the frequency type (e.g., DAY, WEEK, MONTH, YEAR) and value. Enter data on all fields if using this object.

                      <Expandable title="properties">

                        <ParamField body="type" type="string">
                          optional

                          Specifies the frequency type (e.g., DAY, WEEK, MONTH, YEAR).

                        </ParamField>

                        <ParamField body="value" type="string">
                          optional

                          Specifies the frequency value.

                        </ParamField>

                      </Expandable>
                    </ParamField>

                    <ParamField body="availability" type="object">
                      optional

                      Defines a date interval based on starting and ending dates when the subscription is available to use.

                      <Expandable title="properties">

                        <ParamField body="start_at" type="string">
                          optional

                          The start date that the subscription plan will be available to use. If not set, the subscription is available from the moment it is created. `start_at` becomes mandatory when using `finish_at`.

                        </ParamField>

                        <ParamField body="finish_at" type="string">
                          optional

                          The end date until the subscription plan will be available to use. If not defined, the subscription does not have an expiration date. `finish_at` becomes mandatory when using `start_at`.

                        </ParamField>

                      </Expandable>
                    </ParamField>

                  </Expandable>
                </ParamField>

              </Expandable>
            </ParamField>

          </Expandable>
        </ParamField>

        <ParamField body="strong_customer_authentication_exemptions" type="array">
          optional

          Array of Strong Customer Authentication (SCA) exemptions that can be applied to the transaction to reduce friction while maintaining security compliance.

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="vault_on_success" type="boolean">
      optional

      Flag to enroll the card after a successful payment (requires `customer_payer.id`).

    </ParamField>

    <ParamField body="otp" type="object">
      optional

      Specifies the OTP object.

      <Expandable title="properties">

        <ParamField body="is_required" type="boolean">
          optional

          Specifies if a one time password is required in the flow

        </ParamField>

      </Expandable>
    </ParamField>

  </Expandable>
</ParamField>

<ParamField body="subscription" type="object">
  optional

  Specifies the subscription object.

  <Expandable title="properties">

    <ParamField body="frequency" type="object">
      optional

      Specifies the frequency object (used when creating a payment along with the subscription).

      <Expandable title="properties">

        <ParamField body="type" type="enum">
          optional

          Frequency of the subscription payment.

        </ParamField>

        <ParamField body="execution" type="enum">
          optional

          Indicates if the payment is made by Yuno or the merchant.

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="availability" type="object">
      optional

      Specifies the availability object (used when creating a payment along with the subscription).

      <Expandable title="properties">

        <ParamField body="start_at" type="string">
          optional

          Start date of the subscription in UTC time ([ISO 8601](https://en.wikipedia.org/wiki/ISO_8601), MAX 27; MIN 27).

        </ParamField>

        <ParamField body="finish_at" type="string">
          optional

          End date of the subscription in UTC time ([ISO 8601](https://en.wikipedia.org/wiki/ISO_8601), MAX 27; MIN 27).

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="amount" type="object">
      optional

      Specifies the amount object (used when creating a payment along with the subscription).

      <Expandable title="properties">

        <ParamField body="value" type="number">
          optional

          The amount of the subscription payment.

        </ParamField>

        <ParamField body="currency" type="string">
          optional

          The currency used to make the payment. For a full list of currency codes, see [Country reference](/reference/country-reference) (MAX 3; MIN 3; ISO 4217).

        </ParamField>

        <ParamField body="type" type="enum">
          optional

          Type of the subscription payment

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="id" type="string">
      optional

      Unique identifier of the subscription payment (used when creating a payment for an existing subscription).

    </ParamField>

    <ParamField body="billing_date" type="object">
      optional

      Specifies the billing date object (used when creating a payment for an existing subscription).

      <Expandable title="properties">

        <ParamField body="type" type="enum">
          optional

          Type of the billing date

        </ParamField>

        <ParamField body="day" type="string">
          optional

          Day of the month when the subscription payment is made

        </ParamField>

      </Expandable>
    </ParamField>

  </Expandable>
</ParamField>

<ParamField body="callback_url" type="string">
  optional

  The URL where to redirect the customer after the payment. Only required for DIRECT integrations that have a redirection (MAX 526; MIN 3).

</ParamField>

<ParamField body="fraud_screening" type="object">
  optional

  Fraud screening object.

  <Expandable title="properties">

    <ParamField body="stand_alone" type="boolean">
      optional

      Optional field to send in the payment indicating if the fraud screening is stand alone, meaning that a payment will not be excecuted after the fraud screening is made. You can use this field while creating your CARD route.

    </ParamField>

  </Expandable>
</ParamField>

<ParamField body="split_marketplace" type="array">
  optional

  Split marketplace array of objects

  <Expandable title="properties">

    <ParamField body="recipient_id" type="string">
      optional

      The unique identifier of the recipient in the Yuno system. <br/> You must provide the [`recipient_id`](/reference/create-recipient-1) (Yuno-generated) or the `provider_recipient_id` (external provider's ID) when creating a payment.

    </ParamField>

    <ParamField body="provider_recipient_id" type="string">
      optional

      The recipient ID provided by the external payment provider, if applicable. <br/> You must provide the `provider_recipient_id` or the [`recipient_id`](/reference/create-recipients) (Yuno-generated) when creating a payment.

    </ParamField>

    <ParamField body="description" type="string">
      optional

      Description for the split. (MAX 255; MIN 3).

    </ParamField>

    <ParamField body="type" type="enum">
      The type of split. `recipient_id` is mandatory for `PURCHASE` and `MARKETPLACE`.

    </ParamField>

    <ParamField body="merchant_reference" type="string">
      optional

      Optional unique identifier for the split transaction (MAX 255; MIN 3).

    </ParamField>

    <ParamField body="recipient_type" type="enum">
      optional

      The type of recipient for the provider.

      Example: MEAL

    </ParamField>

    <ParamField body="amount" type="object">
      optional

      Defines the amount of the split. This field is optional if the recipient has a `split_configuration` defined, as the amount will be calculated automatically.

      <Expandable title="properties">

        <ParamField body="value" type="number">
          The split amount (multiple of 0.0001).

        </ParamField>

        <ParamField body="currency" type="string">
          The currency used to make the payment. For a full list of currency codes, see [Country reference](/reference/country-reference) (MAX 3; MIN 3; ISO 4217).

        </ParamField>

      </Expandable>
    </ParamField>

    <ParamField body="liability" type="object">
      optional

      Optional information regarding the recipient's liability for fees and chargebacks.

      <Expandable title="properties">

        <ParamField body="processing_fee" type="enum">
          optional

          Indicates who will be charged the transaction fee.

        </ParamField>

        <ParamField body="chargebacks" type="boolean">
          optional

          The recipient is responsible in case of a chargeback.

        </ParamField>

      </Expandable>
    </ParamField>

  </Expandable>
</ParamField>

<ParamField body="metadata" type="array">
  optional

  Custom key–value tags to track this payment or add context (e.g., order_id, invoice, campaign). Up to 120 items.

  <Expandable title="properties">

    <ParamField body="key" type="string">
      The metadata key (MAX 48. Value: MIN:1, MAX:48). **[Case Sensitive]**

    </ParamField>

    <ParamField body="value" type="string">
      The metadata key value (MAX 512. Value: MIN:1, MAX:512). **[Case Sensitive]**

    </ParamField>

  </Expandable>
</ParamField>
