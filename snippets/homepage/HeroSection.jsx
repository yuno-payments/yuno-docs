// HeroSection — deprecated, homepage now uses MDX-native divs with CSS classes
// Kept for backward compatibility if referenced elsewhere.
export const HeroSection = () => null;

// CodeCardHeader — deprecated, homepage now uses .yuno-code-chrome CSS class
export const CodeCardHeader = () => null;

// CodeCardWrapper — deprecated
export const CodeCardWrapper = ({ children }) => children;

// CodeCaption — deprecated
export const CodeCaption = ({ children }) => children;
