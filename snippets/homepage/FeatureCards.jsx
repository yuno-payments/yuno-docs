// PathCards — deprecated, homepage now uses Mintlify CardGroup
// Kept for backward compatibility if referenced elsewhere.
export const PathCards = () => null;

// AgentCallout — deprecated, homepage now uses Mintlify <Info>
export const AgentCallout = () => null;

// FooterLinks — deprecated, homepage now uses .yuno-home-footer CSS class
export const FooterLinks = () => null;
