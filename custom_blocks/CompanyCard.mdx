---
name: CompanyCard
---
export const CompanyCard = ({ title, image, href }) => {
  if (!href) {
    return (
      <div
        className="
          flex
          items-center
          gap-[10px] 
          !p-[0.6rem]
          rounded-[7px]
          text-[0.85rem]
          font-semibold
          cursor-pointer

          /* border: 1px solid var(--yuno-purple-50) */
          border
          border-[color:var(--yuno-purple-50)]

          /* text color: var(--yuno-purple) */
          text-[color:var(--yuno-purple)]
        "
      >
        <div className="flex items-center justify-center h-[23px] w-[30px]">
          <img src={image} alt={`${title} logo`} className="max-h-[23px] max-w-[23px]" />
        </div>
        <span>{title}</span>
      </div>
    );
  }

  return (
    <a
      href={href}
      className="
          flex
          items-center
          gap-[10px] 
          !p-[0.6rem]
          rounded-[7px]
          text-[0.85rem]
          font-semibold
          cursor-pointer

          /* border: 1px solid var(--yuno-purple-50) */
          border
          border-[color:var(--yuno-purple-50)]

          /* text color: var(--yuno-purple) */
          text-[color:var(--yuno-purple)]

          /* small hover “pop” + shadow */
          transition-transform
          duration-100
          hover:scale-[1.02]
        "
    >
      <div className="flex items-center justify-center h-[23px] w-[30px]">
        <img src={image} alt={`${title} logo`} className="max-h-[23px] max-w-[23px]" />
      </div>
      <span>{title}</span>
    </a>
  );
};

<CompanyCard title="Test" image="https://icons.prod.y.uno/pix_logosimbolo.png" href="/docs"/>