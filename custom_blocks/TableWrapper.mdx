---
name: TableWrapper
---
export const TableWrapper = ({ children }) => {
  return (
    <section
      className="
        grid
        grid-cols-3
        gap-[10px]
        max-[800px]:grid-cols-2
      "
    >
      {children}
    </section>
  );
};

<TableWrapper>
	Add a table here.
</TableWrapper>