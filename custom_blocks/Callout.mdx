---
name: Callout
---
export const InfoBlock = props => {
  // define your type variants
  const typeStyles = {
    info: {
      container: 'bg-[#e9e8ff3d] border border-[#3e4fe0]',
      line:      'border-[#3e4fe0]',
    },
    alert: {
      container: 'bg-[#fcf8f2] border border-[#e6c386]',
      line:      'border-[#f0ad4e]',
    },
    danger: {
      container: 'bg-[#fdf7f7] border border-[#ebcccc]',
      line:      'border-[#d9534f]',
    },
    success: {
      container: 'bg-[#f3f8f3] border border-[#c2e6c2]',
      line:      'border-[#50af51]',
    },
  };

  const { title, type = 'info', children } = props;
  const styles = typeStyles[type] || typeStyles.info;

  return (
    <div className={`flex gap-6 p-6 rounded-lg ${styles.container}`}>
      {/* vertical bar */}
      <div className={`border-l-2 self-stretch ${styles.line}`} />

      <div>
        <h3 className="text-neutral-b font-bold leading-8 mb-6">
          {title}
        </h3>
        <div className="flex flex-col gap-6 text-neutral-b font-normal leading-6">
          {children}
        </div>
      </div>
    </div>
  );
};

<InfoBlock title="Test" type="alert">
    You will need the customer's identification information for each subsequent step.
</InfoBlock>