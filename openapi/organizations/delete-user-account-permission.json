{
  "openapi": "3.1.0",
  "info": { "title": "User Account Permissions API - Delete", "version": "1.0.0" },
  "servers": [ { "url": "https://api-sandbox.y.uno/v1" } ],
  "components": {
    "securitySchemes": {
      "sec0": { "type": "apiKey", "in": "header", "name": "PUBLIC-API-KEY", "x-default": "<Your PUBLIC-API-KEY>" },
      "sec1": { "type": "apiKey", "in": "header", "name": "PRIVATE-SECRET-KEY", "x-default": "<Your PRIVATE-SECRET-KEY>" }
    }
  },
  "security": [ { "sec0": [], "sec1": [] } ],
  "paths": {
    "/organizations/users/{user_id}/account-permissions/{account_id}": {
      "delete": {
        "summary": "Delete User Account Permission",
        "operationId": "delete-user-account-permission",
        "description": "Removes a specific account permission from a user.",
        "parameters": [
          { "name": "user_id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" }, "description": "User UUID." },
          { "name": "account_id", "in": "path", "required": true, "schema": { "type": "string", "format": "uuid" }, "description": "Account UUID." }
        ],
        "responses": {
          "204": { "description": "No Content" }
        }
      }
    }
  }
}
